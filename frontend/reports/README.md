# Reports

Machine-generated artifacts are written to:

- `frontend/reports/generated/`

These files are not hand-edited and are ignored by git.

Current generators:

- `npm run report:surface-tree`
- `npm run report:surface-tree:all`
- `npm run report:classname-occurrences`
