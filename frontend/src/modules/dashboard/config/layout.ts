export const TABLE_LAYOUT = {
    // Master Row Height (pixels)
    // 36px = Comfortable (Current)
    // 32px = Dense (Recommended for Native Tool feel)
    // 28px = Ultra-Dense (Excel style)
    rowHeight: 32,

    // Typography for data cells
    fontSize: "text-[11px]",
    fontMono: "font-mono",

    // Icon sizing
    iconSize: 14,

    // Virtualization Overscan
    overscan: 20,
} as const;
