forensic-tailwind-flagged.txt
Timestamp: 2025-12-26T00:00:00Z

Purpose:
- Items here are ambiguous or require manual design decisions before automatic tokenization.

Flagged occurrences (recommend manual review):

1) Width/column literals that lack an existing semantic token:
 - src/modules/dashboard/components/ColumnDefinitions.tsx:211 -> "h-sep w-16 flex-none overflow-visible"  // `w-16` needs a semantic width token (e.g., --tt-col-w-small)

2) Small fixed heights used as layout primitives (may be semantic or content-specific):
 - src/modules/settings/components/SettingsBlockRenderers.tsx:87 -> div.className="flex justify-between items-center h-10"
 - src/modules/settings/components/SettingsModal.tsx:597 -> div.className="p-6 border-b ... h-16 shrink-0"
 - src/modules/dashboard/components/details/tabs/TrackersTab.tsx:36 -> GlassPanel className contains `h-lg` now (was h-48) — verify visual intent

3) Tight/utility heights used for skeletons and progress bars:
 - src/modules/dashboard/components/TorrentTable.tsx:1823..1839 -> multiple `h-3` / `h-4` spans used for skeletons and progress bars — confirm mapping to `h-sep` or dedicated tokens

4) Misc text-size bracket usages previously converted but still worth review for semantic naming:
 - src/modules/dashboard/components/details/visualizations/PeerMap.tsx:189 -> text-10px
 - src/modules/dashboard/components/details/visualizations/PeerMap.tsx:216 -> text-9px
 - src/modules/dashboard/components/details/tabs/PeersTab.tsx:169 -> text-10px

Notes / recommended actions:
- Create specific width tokens for repeated `w-*` usages (e.g., `--tt-col-w-small`) instead of mapping to an arbitrary existing token.
- Map skeleton/progress heights to one semantic token (e.g., `--tt-skeleton-h`) and add `.h-skeleton` utility.
- Confirm the visual intent for `h-10`, `h-16` and similar values — if they represent button sizes or header bars, map to existing `--button-h`, `--tt-h-nav`, etc.

Once reviewed, I can proceed to apply the manual mappings.
