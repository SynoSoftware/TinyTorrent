<?xml version="1.0" encoding="utf-8" ?>
<Window
    x:Class="Synapse_Sample.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="using:Synapse_Sample"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    Title="Synapse.Sample"
    mc:Ignorable="d">
    <Window.SystemBackdrop>
        <MicaBackdrop />
    </Window.SystemBackdrop>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TitleBar x:Name="AppTitleBar" Title="Synapse.Sample">
            <TitleBar.IconSource>
                <ImageIconSource ImageSource="Assets/AppIcon.ico" />
            </TitleBar.IconSource>
        </TitleBar>

        <!--
            Two consumers, deliberately of different shapes. AGENTS.md: one consumer proves
            nothing, so the control is built against a downloads list and a departures board.
        -->
        <SelectorBar x:Name="Pages"
                     Grid.Row="1"
                     Margin="12,0,12,0"
                     SelectionChanged="OnPageSelected">
            <SelectorBarItem Text="Downloads" IsSelected="True" />
            <SelectorBarItem Text="Departures" />
        </SelectorBar>

        <Frame x:Name="RootFrame" Grid.Row="2" />
    </Grid>
</Window>
